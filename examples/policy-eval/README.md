# policy-eval

A small IAM / gateway authz extension. Demonstrates how to mount a
typed policy-decision handler against the zip extension surface.

```bash
npm install
npm run build      # writes ./policy_eval.js next to extension.json
```
